import React from 'react';
import Navigation from '../navigation/Navigation.jsx';
import './Header.css';

const Header = () => {
  return (
    <header>
      <Navigation />
    </header>
  );
};

export default Header;
